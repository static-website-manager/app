- if form.object.errors[:content_role].any?
  p.has-error.has-feedback: strong.control-label Content Role
- else
  p: strong Content Role
.radio
  = form.label :content_role_writer do
    => form.radio_button :content_role, :writer
    | Writer
    small.help-block These users can only write Drafts and Posts.
.radio
  = form.label :content_role_editor do
    => form.radio_button :content_role, :editor
    | Editor
    small.help-block These users can additionally manage Pages, Collections, Data and Files.
.radio
  = form.label :content_role_designer do
    => form.radio_button :content_role, :designer
    | Designer
    small.help-block These users can additionally manage Design files, as well as setup external Deployments and Form Responders.
hr
p: strong Branch Access
p.text-muted.m-b All team members have access to their own staging branch. Users may be given access to other branches, including the production branch and other team members’ staging branches.
.checkbox
  = form.label :production_branch_access do
    => form.check_box :production_branch_access
    | Production Branch
    small.help-block Access to the primary (master) production branch.
.checkbox
  = form.label :staging_branch_access do
    => form.check_box :staging_branch_access
    | Staging Branches
    small.help-block Access to team staging branches.
.checkbox
  = form.label :custom_branch_access do
    => form.check_box :custom_branch_access
    | Custom Branches
    small.help-block Includes the ability to add, rename and delete custom branches.
- if defined?(advanced_options) && !!advanced_options
  hr
  p: strong Advanced Options
  .checkbox
    = form.label :account_owner do
      => form.check_box :account_owner
      | Account Owner
      small.help-block These users can manage the Team Roster and the basic Website Settings, including the website’s subscription with Static Website Manager.
  .checkbox
    = form.label :ssh_access do
      => form.check_box :ssh_access
      | SSH Access
      small.help-block These users can push to the website repository over SSH. (Note: All operations are allowed when using SSH Access, regardless of Content Role or Branch Access.)
