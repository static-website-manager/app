- @_body_container = false
- title "New Post • #{@branch.title(current_user)}"

= form_for [@website, @branch, @post] do |form|
  .blob-container
    .blob-header
      .container-fluid
        => link_to [@website, @branch, :posts], class: 'logo-small-back-container' do
          => icon 'reply'
          => image_tag 'logo-small.png', alt: 'Static Website Manager', class: 'logo-small'
        ul.nav.nav-tabs
          li.active = link_to 'New Post', '#'
    .container-fluid
      .blob-content
        = render partial: 'blobs/form', object: form, locals: { placeholder: 'Add New Post' }
