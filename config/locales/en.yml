en:
  activerecord:
    attributes:
      authorization:
        role: Content Role
        owner: Account Owner
      deployment/s3:
        aws_s3_bucket: S3 Bucket Name
        aws_region: Bucket Region
        aws_access_key_id: AWS Access Key ID
        aws_secret_access_key: AWS Secret Access Key
      form_responder:
        dataset_pathname: Data Connection
        email_addresses: Email Addresses
      user:
        password_confirmation: Password Confirmation
      website:
        name: Website Name
    errors:
      models:
        website:
          attributes:
            subscription_plan:
              users_exceeded: does not allow enough users to match your current team (%{user_count} users) - please downsize your team first
  date:
    formats:
      default: '%B %d, %Y'
